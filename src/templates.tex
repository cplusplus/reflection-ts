%!TEX root = ts.tex

\rSec0[temp]{Templates}

\setcounter{section}{5}
\rSec1[temp.res]{Name resolution}

\setcounter{subsection}{1}
\rSec2[temp.dep]{Dependent names}

\rSec3[temp.dep.type]{Dependent types}

\pnum
In {\cppstddocno} [temp.dep.type], apply the following changes to paragraph \cxxref{temp.dep.type.paragraph1}:

\setcounter{Paras}{\cxxref{temp.dep.type.paragraph1}}
\setcounter{Bullets1}{7}

\begin{std.txt}

A type is dependent if it is \newline
[...]

\begin{itemize}
  \item denoted by \tcode{decltype(}\grammarterm{expression}{}\tcode{)}, where
        \grammarterm{expression} is type-dependent (\cxxref{temp.dep.expr})\removed{.}\added{, or}

  \item \added{denoted by \tcode{reflexpr(}\emph{operand}{}\tcode{)}, where \grammarterm{operand} is a type-dependent expression or a (possibly parenthesized) \grammarterm{functional-type-conv-expression} with at least one type-dependent immediate subexpression, or}

  \item \added{denoted by \tcode{reflexpr(}\emph{operand}{}\tcode{)},
  where \grammarterm{operand} designates a dependent type or a member of an unknown
  specialization or a value-dependent constant expression.}
\end{itemize}
\end{std.txt}

\rSec0[except]{Exception handling}

For Exception handling, {\cppstddocno}, Clause \the\value{chapter} shall be used.

\rSec0[cpp]{Preprocessing directives}

For Preprocessing directives, {\cppstddocno}, Clause \the\value{chapter} shall be used.
